[pytest]
# pytest-asyncio: tests in test_main_async.py are decorated with
# @pytest.mark.asyncio, so strict mode (the default) is correct and explicit
# here to silence the "unset asyncio_mode" warning in CI.
asyncio_mode = strict
testpaths = tests
